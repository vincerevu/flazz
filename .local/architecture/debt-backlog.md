# Debt Backlog

## UI / UX
- **Image Upload:** Implement real image upload and preview instead of placeholder `![${file.name}](pending-upload)` (see `apps/renderer/src/App.tsx`).

## Architecture & Code Quality
- Monitor adherence to the new `performance-budget.md` during feature development.
- Refine telemetry logging across any newly added providers or integrations.
