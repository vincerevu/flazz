// Skill loading is handled through the shared SkillRegistry.
// Built-in skills live in source, workspace skills live under ~/Flazz/memory/Skills,
// and both can be resolved through the loadSkill tool at runtime.
